git checkout master
git pull origin master
git checkout ocl
git pull origin ocl
git checkout sql
git pull origin sql
git checkout report
git pull origin report
git checkout XML_imports
git pull origin XML_imports
git checkout master
